version: '1.0'

steps:
  build:
    type: build
    description: Free text description]
    dockerfile: Dockerfile
    image-name: shay/antenna
    tag: develop

  unit-test-step:
    image: shay/antenna

    working-directory: ./test
    commands:
        - gulp unit_pre

