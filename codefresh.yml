version: '1.0'

steps:
  build:
    type: build
    description: Free text description]
    dockerfile: Dockerfile
    image-name: shay/antenna
    tag: develop

  unit-test-step:
    image: codefresh/buildpacks:all-5

    working-directory: ${{initial-clone}}
    commands:
        - npm install
        - gulp unit_pre

